import { combineReducers } from "redux";

// Define the reducers you want to import from the reducers file.
import { } from './reducers'

export default combineReducers({
    // Insert imported reducers here
});